CREATE TABLE IF NOT EXISTS Gallery(id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT, UNIQUE(name));
CREATE TABLE IF NOT EXISTS Picture(id INTEGER PRIMARY KEY AUTOINCREMENT, url TEXT, gallery_id INTEGER, name TEXT, description TEXT, artist TEXT, FOREIGN KEY(gallery_id) REFERENCES Gallery(id), UNIQUE(gallery_id, url));